# Lab 17: Default Route & Gateway of Last Resort — Solution

> ⚠️ **Try it yourself before looking at this!**

## Key Commands

*Solution will be added after completing the lab.*

## Successful Verification

- [ ] `show ip route (R1 — verify * flag)` — OK
- [ ] `ping from LAN to ISP loopback` — OK

## Notes

Default route (`0.0.0.0/0`) = 'send everything you don't recognize toward the ISP'. Flag `S*` = static default.
